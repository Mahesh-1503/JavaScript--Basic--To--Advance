let options = {
    title: 'My Menu',
    items: ['Home', 'About', 'Contact']
}

function showMenu({title, items}){
    console.log(title);
    console.log(items);
}

showMenu(options);

/**
 * BrushUp the concept with multiple examples of function destructuring
 */