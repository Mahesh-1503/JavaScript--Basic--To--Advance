// Task: use array destructuring to extract the first
//three colors from  the colors array and assign them to
// separate variables named color1, color2, and color3

//After extracting the colors, log each variable's value to the console.

const colors = ["red", "blue", "green", "yellow", "black"];
const [color1, color2, color3] = colors;
console.log(color1, color2, color3);

/**
 * continue watching from 5:44:30
 * next starts ==> object destructuring
 */
