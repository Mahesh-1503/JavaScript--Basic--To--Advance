/**
 * A function is a block of code designed to perform a particular task.
 * A JavaScript function is executed when "something" invokes it (calls it).
 * Functions are used to break down a complex task into simple steps.
 * Functions can be used to perform the same action multiple times.Reuse of functions saves time.You can declare it once and use it multiple times.
 * DRY = Don't Repeat Yourself
 * function name(parameter1,parameter2){code to be executed}
 */


function greet(){
    console.log("Hello, World!"); // code to be executed
}

//call,run or invoke the function
greet();