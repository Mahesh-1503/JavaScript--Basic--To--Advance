/**
 * setInterval() and setTimeout()
 * - setInterval() is used to repeat a function at a specified interval.
 * - setTimeout() is used to execute a function after a specified delay.
 */

setInterval(() => {
    console.log('Hello world');
}, 1000); // This will print 'Hello world' every second.
